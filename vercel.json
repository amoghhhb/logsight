{
  "functions": {
    "Website/api/summarize.js": {
      // Use the clean Vercel Node.js syntax
      "runtime": "nodejs20.x" 
      // This is the Vercel-preferred standard when specifying a version.
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "Website/api/summarize.js"
    }
  ]
}
