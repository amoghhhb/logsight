{
  "name": "logsight-summarizer",
  "version": "1.0.0",
  "description": "AI-Powered Log Summarizer (Vercel Node.js)", 
  "main": "Website/api/summarize.js", // <-- Property
  "scripts": { // <-- Property
    "start": "node Website/api/summarize.js"
  }, // <-- THIS MUST BE FOLLOWED BY A COMMA (,)
  "dependencies": { // <-- This key MUST be surrounded by DOUBLE QUOTES
    "@google/genai": "^0.15.0"
  }, // <-- THIS MUST BE FOLLOWED BY A COMMA (,)
  "engines": { // <-- This key MUST be surrounded by DOUBLE QUOTES
    "node": "20.x"
  }
}
